generator client {
  provider     = "prisma-client"
  output       = "../src/generated/prisma"
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
}

// Domain models and the first migration are intentionally added with the first
// product capability. Placeholder persistence would create false architecture.
